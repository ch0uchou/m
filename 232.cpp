template<class T, class U>
U Func(T var_x, U var_y){
    cout << var_x << << var_y;
}
//A. A function taking a single generic parameter and returning a specific non-void type
//B. A function taking a single generic parameter and returning a generic type which must be different from argument type
//C. All of these 
//D. A function taking a single generic parameter and returning a generic type which may be different from argument type
//E. A funciton taking a single generic parameter and returning a generic type which must have the same type as argument type
//Chon D